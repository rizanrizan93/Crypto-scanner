name: Crypto Strategy Lab Refinement

on:
  push:
    branches:
      - main
    paths:
      - "research/strategy_lab_refinement.py"
      - ".github/workflows/strategy-lab-refinement.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: crypto-strategy-lab-refinement
  cancel-in-progress: true

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install
        run: python -m pip install --upgrade pip && pip install -e .
      - name: Run refinement lab
        run: python research/strategy_lab_refinement.py --output strategy_lab_refinement_results.json
      - name: Upload research artifact
        uses: actions/upload-artifact@v4
        with:
          name: strategy-lab-refinement-results
          path: strategy_lab_refinement_results.json
          retention-days: 7
